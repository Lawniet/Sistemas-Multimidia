function createCanvas(app, width, height){
}

window.onload = () => {
    const app = document.getElementById('main');
    const gameWindow = createCanvas(app, 513, 513);
    const gameContext = gameWindow.getContext('2d');
    const gameDiv = 20;
}